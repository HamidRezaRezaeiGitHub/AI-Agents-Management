# Claude Code Project Memory

Follow `AGENTS.md` at the root of the repository for project instructions, and route every request through `ai/workflows/workflow-dispatch.md` before reading other workflows.

## Claude Notes

- Add project commands under `.claude/commands/` only when they are reusable by the team.
- Add project subagents under `.claude/agents/` only for recurring specialized workflows.
