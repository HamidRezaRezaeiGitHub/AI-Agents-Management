# Agents

Use this directory for reusable subagent definitions and role prompts.

Agent guidance:

- Give each agent a narrow purpose.
- State when it should be used.
- Limit tools when the role does not need broad access.
- Keep implementation agents separate from read-only review agents.
