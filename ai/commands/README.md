# Commands

Use this directory for reusable prompt commands and slash-command assets.

Command guidance:

- One command should do one recurring job.
- Include arguments only when they reduce ambiguity.
- Keep commands short enough to inspect quickly.
- Prefer commands for explicit workflows and skills for discoverable capabilities.
